<h3>Import data</h3>
<p><b>IMPORTANT</b></p>
<p>This tool will allow you to import data from other genealogy sources.<br/>
    Currently supported formats are Gramps XML and GEDCOM 5.<br/>
    No attempt will be made to match existing data with imported data, so be careful when importing files that you know have duplicate data.</p>
<%= form_tag('/import/', multipart: true, :accept => 'text/xml') do %>
<input type="hidden" id="type" name="type" value="gedcom">
<div class="form-horizontal">
    <div class="form-group">
        <%= label_tag :file, 'Select file to import', :class => "col-sm-2 control-label" %>
        <div class="input-group">
            <%= file_field_tag :file, :class => "form-control" %>
            <div class="input-group-btn">
                <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">GEDCOM 5 <span class="caret"></span></button>
                <ul class="dropdown-menu">
                    <li><a href="#">GEDCOM 5</a></li>
                    <li><a href="#">Gramps XML</a></li>
                </ul>
                <%= submit_tag "Upload", :class => "btn btn-default" %>
            </div> 
        </div>
    </div>
</div>

<% end %>