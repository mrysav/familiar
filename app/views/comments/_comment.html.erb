<% user = User.find(comment.owner_id) %>
<%= comment.body.html_safe %><br>
<em class="small">
    <%= user.name %> - <%= comment.cool_date %>
    <% if current_user == user %>
    (<%= link_to :delete, [comment.photo, comment],
                 method: :delete,
                 data: { confirm: 'Are you sure?' } %>)
    <% end %>
</em>
<hr>